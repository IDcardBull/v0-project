<view class="container od-container" wx:if="{{order}}">
  <!-- 状态条 -->
  <view class="od-status">
    <view class="od-status-title" style="color: {{order.statusColor}}">{{order.statusText}}</view>
    <view class="od-status-sub">
      <text wx:if="{{order.status === 'pending_pay'}}">客服将尽快与您确认价格与库存</text>
      <text wx:elif="{{order.status === 'pending_ship'}}">订单已确认，正在备货</text>
      <text wx:elif="{{order.status === 'shipped'}}">已发货{{order.trackingNo ? ' · ' + order.trackingNo : ''}}</text>
      <text wx:elif="{{order.status === 'completed'}}">订单已完成，感谢您的下单</text>
      <text wx:elif="{{order.status === 'after_sale'}}">售后处理中，客服会主动联系您</text>
      <text wx:elif="{{order.status === 'closed'}}">订单已关闭</text>
    </view>
  </view>

  <!-- 收货地址 -->
  <view class="od-address card" wx:if="{{order.address}}">
    <view class="od-addr-main">
      <text class="od-addr-name">{{order.address.receiver}}</text>
      <text class="od-addr-phone">{{order.address.phone}}</text>
    </view>
    <view class="od-addr-line">
      {{order.address.province}}{{order.address.city}}{{order.address.district || ''}} {{order.address.detail}}
    </view>
  </view>

  <!-- 商品 -->
  <view class="od-goods card">
    <view wx:for="{{order.items}}" wx:key="skuId" class="od-goods-item">
      <image src="{{item.image}}" class="od-goods-img" mode="aspectFill" wx:if="{{item.image}}" />
      <view class="od-goods-img cart-img-empty" wx:else>暂无图</view>
      <view class="od-goods-body">
        <view class="od-goods-name ellipsis-2">{{item.name}}</view>
        <view class="od-goods-spec" wx:if="{{item.spec}}">{{item.spec}}</view>
        <view class="od-goods-meta">¥{{item.priceText}} × {{item.qty}}</view>
      </view>
      <view class="od-goods-total">¥{{item.lineTotalText}}</view>
    </view>

    <view class="od-amount-row">
      <text>商品金额</text>
      <text>¥{{order.goodsAmountText}}</text>
    </view>
    <view class="od-amount-row" wx:if="{{order.discountAmountText}}">
      <text>优惠</text>
      <text>-¥{{order.discountAmountText}}</text>
    </view>
    <view class="od-amount-row">
      <text>运费</text>
      <text wx:if="{{order.freight && order.freight > 0}}">¥{{order.freightText}}</text>
      <text wx:else class="od-ship-free">客服核算</text>
    </view>
    <view class="od-amount-row od-amount-row-final">
      <text>订单合计</text>
      <text class="od-total">¥{{order.totalAmountText}}</text>
    </view>
  </view>

  <!-- 订单信息 -->
  <view class="od-meta card">
    <view class="od-meta-row">
      <text class="od-meta-label">订单编号</text>
      <view class="od-meta-value-wrap">
        <text class="od-meta-value">{{order.orderNo || order.id}}</text>
        <text class="od-copy" bindtap="copyOrderNo">复制</text>
      </view>
    </view>
    <view class="od-meta-row">
      <text class="od-meta-label">下单时间</text>
      <text class="od-meta-value">{{order.createdAtText}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.paidAtText}}">
      <text class="od-meta-label">支付时间</text>
      <text class="od-meta-value">{{order.paidAtText}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.shippedAtText}}">
      <text class="od-meta-label">发货时间</text>
      <text class="od-meta-value">{{order.shippedAtText}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.completedAtText}}">
      <text class="od-meta-label">完成时间</text>
      <text class="od-meta-value">{{order.completedAtText}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.closedAtText}}">
      <text class="od-meta-label">关闭时间</text>
      <text class="od-meta-value">{{order.closedAtText}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.logisticsCompany}}">
      <text class="od-meta-label">物流公司</text>
      <text class="od-meta-value">{{order.logisticsCompany}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.trackingNo}}">
      <text class="od-meta-label">物流单号</text>
      <view class="od-meta-value-wrap">
        <text class="od-meta-value">{{order.trackingNo}}</text>
      </view>
    </view>
    <view class="od-meta-row" wx:if="{{order.payMethod}}">
      <text class="od-meta-label">支付方式</text>
      <text class="od-meta-value">{{order.payMethod === 'offline' ? '线下结算' : (order.payMethod === 'wechat' ? '微信支付' : (order.payMethod === 'credit' ? '授信结算' : order.payMethod))}}</text>
    </view>
    <view class="od-meta-row" wx:if="{{order.remark}}">
      <text class="od-meta-label">订单备注</text>
      <text class="od-meta-value">{{order.remark}}</text>
    </view>
  </view>

  <view class="bar-placeholder"></view>

  <!-- 底部栏 -->
  <view class="bottom-bar">
    <view class="btn btn-outline bb-btn" bindtap="callService">联系客服</view>
    <view wx:if="{{order.status === 'pending_pay' || order.status === 'pending_ship'}}" class="btn btn-primary bb-btn" bindtap="cancelOrder">取消订单</view>
    <view wx:if="{{order.status === 'shipped'}}" class="btn btn-primary bb-btn" bindtap="confirmReceive">确认收货</view>
  </view>
</view>

<view wx:elif="{{loading}}" class="empty"><text class="empty-sub">加载中…</text></view>
